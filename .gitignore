# dependencies
node_modules/
.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production build
/build
/dist

# environment: the Supabase URL and keys live here. Everything but the example is ignored,
# so a stray .env.backup or .env.production cannot be committed by accident.
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo

# Aeonik is licensed from CoType Foundry. The binaries stay out of this repo.
/fonts/
/public/fonts/

# local dev output, per CLAUDE.md: the worklog and progress shots are a local handoff
docs/log.md
docs/progress/

# testing
/coverage

# supabase local state
supabase/.temp/
supabase/.branches/

# macOS
.DS_Store

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
bun-debug.log*
